name: cochlea-net

channels:
  - conda-forge

dependencies:
  - cluster_tools
  - mobie_utils
  - pip
  - pooch
  - pybdv
  - pytorch
  - scikit-image
  - s3fs
  - torch_em
  - trimesh
  - z5py
  # Don't install zarr v3, which is not yet compatible with all dependencies.
  - zarr <3
