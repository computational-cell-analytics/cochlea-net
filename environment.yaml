name: cochlea-net

channels:
  - conda-forge

dependencies:
  - cluster_tools
  - scikit-image
  - pip
  - pooch
  - pybdv
  - pytorch
  - s3fs
  - torch_em
  - trimesh
  - z5py
  # Don't install zarr v3, which is not yet compatible with all dependencies.
  - zarr <3
